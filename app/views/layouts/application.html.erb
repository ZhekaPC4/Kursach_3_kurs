<!DOCTYPE html>
<html>
  <head>
    <title>СБЭУ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  
  <div class="site_header">
    <a class="site_header_button" href=<%= main_page_path %>>На главную страницу</a>
    <% if current_user.present? %>  
      <a class="site_header_button" href=<%= user_path(current_user.id) %>>Профиль</a>
      <a class="site_header_button" href=<%= order_bucket_path %>>Корзина</a>
      <a class="site_header_button" href=<%= order_index_path %>> <% if current_user.role_id == 2 %> CMS заказов <% else %> Мои заказы <% end %> </a> 
      <a class="site_header_button" href=<%= user_log_out_path %>>Выйти из аккаунта</a>
      <% if current_user.role_id == 2 %>  
        <a class="site_header_button" href=<%= tea_new_path %>>Новый чай</a>
        <a class="site_header_button" href=<%= tea_cms_path %>>CMS чая</a> 
      <% end %>
      <% if current_user.role_id == 1 %>  
        <a class="site_header_button" href=<%= user_index_path %>>Пользователи</a>
      <% end %>
    <% else %>  
      <a class="site_header_button" href=<%= user_login_path %>>Авторизация</a>
      <a class="site_header_button" href=<%= user_new_path %>>Регистрация</a>
    <% end %>

  </div>
  
  <body>
    <%= yield %>
  </body>
</html>
