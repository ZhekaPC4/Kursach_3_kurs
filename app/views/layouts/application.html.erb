<!DOCTYPE html>
<html>
  <head>
    <title>Новости</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  
  <div class="site_header">
    <a class="to_main_page" href=<%= main_page_path %>>На главную страницу</a>
    <% if current_user.present? %>  
      <a class="to_user" href=<%= user_path(current_user.id) %>>Профиль</a>
      <a class="to_user_log_out" href=<%= user_log_out_path %>>Выйти из аккаунта</a>
    <% else %>  
      <a class="to_user_page" href=<%= user_login_path %>>Авторизация</a>
      <a class="to_create_page" href=<%= user_new_path %>>Регистрация</a>
    <% end %>
  </div>
  <style>
    .to_main_page{
      position: relative;
      margin: 0px  20px;
      padding: 6px;
      border: solid black;
      text-decoration: none;
      color: black;
      height: 100px;
    }
    .site_header{
       margin: 30px 0px;
    }
  </style>

  <body>
    <%= yield %>
  </body>
</html>
